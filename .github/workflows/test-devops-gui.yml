name: Test DevOps GUI Installer

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test-devops-gui:
    runs-on: macos-latest
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Homebrew, pipx, and gum
        run: |
          if ! command -v brew &>/dev/null; then
            NONINTERACTIVE=1 /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
          fi
          brew update
          brew install pipx
          pipx ensurepath
          brew install charmbracelet/tap/gum

      - name: Configure shell environment
        run: |
          # Ensure Homebrew is available in PATH
          eval "$(/opt/homebrew/bin/brew shellenv)"
          export PATH="$HOME/.local/bin:$PATH"
          echo "🔧 PATH = $PATH"

          # Explicitly add brew cask path for GUI tools
          export PATH="/opt/homebrew/bin:$PATH"
          export PATH="/opt/homebrew/Caskroom:$PATH"
          echo "🔧 Updated PATH = $PATH"

      - name: Run setup-devops-gui.sh with --all
        shell: bash
        run: |
          chmod +x setup-devops-gui.sh
          bash setup-devops-gui.sh --all

      - name: Verify GUI tools via CLI
        run: |
          GUI_CLI_TOOLS=(
            docker
            gcloud
            code
            iterm2
            tailscale
            ngrok
          )

          echo "🔍 Проверка GUI-инструментов по CLI..."
          FAIL=0
          for tool in "${GUI_CLI_TOOLS[@]}"; do
            echo "🔧 Проверка: $tool"
            if ! command -v "$tool" &>/dev/null; then
              echo "❌ $tool НЕ ДОСТУПЕН"
              FAIL=1
            else
              echo "✅ $tool доступен"
            fi
          done

          if [[ "$FAIL" -ne 0 ]]; then
            echo "❌ Некоторые GUI-инструменты не установлены или не работают"
            exit 1
          fi

      - name: Success
        if: success()
        run: echo "🎉 Все GUI-инструменты успешно установлены и проверены!"
