name: Test Full DevOps Installer (GUI + CLI + Configs)

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test-macos-full:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Prepare brew, pipx, gum
        run: |
          brew update
          brew install pipx
          pipx ensurepath
          brew install charmbracelet/tap/gum

      - name: Set PATHs
        run: |
          echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.bash_profile
          eval "$(/opt/homebrew/bin/brew shellenv)"
          export PATH="$HOME/.local/bin:$PATH"

      - name: Make script executable and run full install
        run: |
          chmod +x setup-devops.sh
          ./setup-devops.sh --all

      - name: Verify installed CLI tools
        run: |
          TOOLS=(
            terraform terragrunt terraform-docs tfsec
            kubectl helm k9s kind
            aws az gcloud
            glab docker lazygit
            bat fzf htop ncdu tree
            python pipx pre-commit
            doctl flyctl sops tldr yq eza
          )

          echo "🔍 Проверка CLI инструментов..."
          FAIL=0
          for tool in "${TOOLS[@]}"; do
            if ! command -v "$tool" &>/dev/null; then
              echo "❌ $tool НЕ установлен!"
              FAIL=1
            else
              echo "✅ $tool установлен"
            fi
          done

          if [[ $FAIL -ne 0 ]]; then
            echo "❌ Некоторые CLI-инструменты отсутствуют!"
            exit 1
          fi

      - name: Проверка GUI инструментов (опционально)
        continue-on-error: true
        run: |
          GUI_CASKS=(
            docker
            google-cloud-sdk
            visual-studio-code
            iterm2
            tailscale
            ngrok
            pgadmin4
            db-browser-for-sqlite
            lens
          )

          echo "🖥️ Проверка GUI тулзов (необязательная в CI)..."
          for cask in "${GUI_CASKS[@]}"; do
            if brew list --cask "$cask" &>/dev/null; then
              echo "✅ $cask установлен"
            else
              echo "⚠️ $cask не установлен (и это ОК в CI-среде)"
            fi
          done

      - name: Проверка конфигурации Oh My Zsh
        run: |
          echo "🧪 Проверка ~/.zshrc и Powerlevel10k..."
          [[ -f ~/.zshrc ]] && echo "✅ ~/.zshrc найден" || (echo "❌ ~/.zshrc отсутствует" && exit 1)
          grep -q "powerlevel10k/powerlevel10k" ~/.zshrc && echo "✅ Powerlevel10k активирован" || (echo "❌ Тема не активирована" && exit 1)

          echo "📦 Проверка Oh My Zsh плагинов..."
          PLUGINS=(
            zsh-autosuggestions
            zsh-syntax-highlighting
            zsh-completions
            zsh-z
            fzf
            command-not-found
            alias-finder
            colored-man-pages
            extract
            history
            safe-paste
            common-aliases
          )
          for p in "${PLUGINS[@]}"; do
            [[ -d "$HOME/.oh-my-zsh/custom/plugins/$p" ]] && echo "✅ $p установлен" || echo "⚠️ $p не найден (возможно встроен)"
          done

      - name: Проверка DevOps алиасов
        shell: zsh {0}
        run: |
          echo "🔎 Проверка DevOps алиасов..."
          ALIASES=(k kgp kgn tf dc dcu dcd lg p)
          for a in "${ALIASES[@]}"; do
            if alias "$a" &>/dev/null; then
              echo "✅ alias $a настроен"
            else
              echo "⚠️ alias $a НЕ найден (возможно не в текущей сессии)"
            fi
          done

      - name: Проверка Neovim конфигурации и Lazy.nvim
        run: |
          echo "🧪 Проверка Neovim и Lazy.nvim..."
          command -v nvim || (echo "❌ Neovim не найден" && exit 1)
          [[ -f ~/.config/nvim/init.lua ]] && echo "✅ init.lua найден" || (echo "❌ init.lua не найден" && exit 1)

          echo "⚡️ Lazy.nvim sync..."
          nvim --headless "+Lazy! sync" +qa || echo "⚠️ Lazy.nvim sync завершился с ошибкой (возможно приватные плагины)"

      - name: Успех!
        if: success()
        run: echo "🎉 Установка и конфигурация DevOps-окружения успешно протестирована!"
