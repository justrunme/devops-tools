name: Test Universal DevOps Installer

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test-devops:
    runs-on: macos-latest
    defaults:
      run:
        shell: bash

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Homebrew, pipx and gum
        run: |
          if ! command -v brew &>/dev/null; then
            NONINTERACTIVE=1 /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
          fi
          brew update
          brew install pipx
          pipx ensurepath
          brew install charmbracelet/tap/gum

      - name: Run setup-devops.sh with --all
        run: |
          chmod +x setup-devops.sh
          ./setup-devops.sh --all --debug

      - name: Verify CLI tools installed
        run: |
          CLI_TOOLS=(
            git zsh fzf jq bat tree
            kubectl helm k9s terraform
            aws az gh glab pipx ansible
            act direnv zoxide httpie cheat btop
          )

          echo "🔍 Проверка CLI-инструментов..."
          FAIL=0
          for tool in "${CLI_TOOLS[@]}"; do
            if ! command -v "$tool" &>/dev/null; then
              echo "❌ $tool НЕ УСТАНОВЛЕН"
              FAIL=1
            else
              echo "✅ $tool установлен"
            fi
          done

          if [[ "$FAIL" -ne 0 ]]; then
            echo "❌ Некоторые CLI-инструменты не установлены"
            exit 1
          fi

      - name: Verify GUI apps installed
        run: |
          GUI_PATHS=(
            "/Applications/Docker.app"
            "/Applications/Google Cloud SDK"
            "/Applications/Visual Studio Code.app"
            "/Applications/iTerm.app"
            "/Applications/Tailscale.app"
            "/Applications/Ngrok.app"
          )

          echo "🔍 Проверка GUI-приложений..."
          FAIL=0
          for path in "${GUI_PATHS[@]}"; do
            if [ ! -e "$path" ]; then
              echo "❌ Не найдено: $path"
              FAIL=1
            else
              echo "✅ Найдено: $path"
            fi
          done

          if [[ "$FAIL" -ne 0 ]]; then
            echo "❌ Некоторые GUI-инструменты не установлены"
            exit 1
          fi

      - name: Success
        if: success()
        run: echo "🎉 Все DevOps-инструменты (CLI + GUI) успешно установлены и проверены!"
